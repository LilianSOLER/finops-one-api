diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index 0c40531..fc1cbff 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -105,32 +105,30 @@ enum UserRole {
   USER
 }
 
-model AwsMetrics{
-    id String @id @default(uuid())
-    timePeriod DateTime?
-    service String?
-    amortizedCost Float?
-    blendedCost Float?
-    unblendedCost Float?
-    netUnblendedCost Float?
-    netAmortizedCost Float?
-    normalizedUsageAmount Int?
-    usageQuantity Int?
-    // TODO: Add link to project table
-
-    project Project @relation(fields:[projectId], references:[id])
-    projectId String? @unique
+model AwsMetrics {
+  id                    String    @id @default(uuid())
+  timePeriod            DateTime?
+  service               String?
+  amortizedCost         Float?
+  blendedCost           Float?
+  unblendedCost         Float?
+  netUnblendedCost      Float?
+  netAmortizedCost      Float?
+  normalizedUsageAmount Int?
+  usageQuantity         Int?
+
+  project   Project @relation(fields: [projectId], references: [id])
+  projectId String  @unique
 }
 
-model AwsCredentials{
-  id String @id @default(uuid())
-  createdAt DateTime
-  accessKeyId String @unique
+model AwsCredentials {
+  id              String   @id @default(uuid())
+  createdAt       DateTime
+  accessKeyId     String   @unique
   secretAccessKey String
 
-    // TODO : Add account, user ou project for the associated credentials
-    project Project @relation(fields:[projectId], references:[id])
-    projectId String @unique
+  project   Project @relation(fields: [projectId], references: [id])
+  projectId String  @unique
 }
 
 enum CompanyRole {
